//
//  Colors.swift
//  AccorAllDesignSystem
//
//  Generated automatically from design tokens — do not edit by hand.
//

import SwiftUI

public extension Color {
    // MARK: - Accent

    static let accentContainerLow = Color("accentContainerLow", bundle: .designSystem)
    static let accentContainerHi = Color("accentContainerHi", bundle: .designSystem)
    static let onAccentContainer = Color("onAccentContainer", bundle: .designSystem)
    static let accent = Color("accent", bundle: .designSystem)
    static let onAccent = Color("onAccent", bundle: .designSystem)
    static let outlineAccent = Color("outlineAccent", bundle: .designSystem)

    // MARK: - All

    static let allPrimary = Color("allPrimary", bundle: .designSystem)

    // MARK: - Danger

    static let dangerContainerLow = Color("dangerContainerLow", bundle: .designSystem)
    static let dangerContainerHi = Color("dangerContainerHi", bundle: .designSystem)
    static let onDangerContainerLow = Color("onDangerContainerLow", bundle: .designSystem)
    static let onDangerContainerHi = Color("onDangerContainerHi", bundle: .designSystem)
    static let danger = Color("danger", bundle: .designSystem)
    static let onDanger = Color("onDanger", bundle: .designSystem)
    static let outlineDanger = Color("outlineDanger", bundle: .designSystem)

    // MARK: - Eco

    static let ecoContainerLow = Color("ecoContainerLow", bundle: .designSystem)
    static let ecoContainerHi = Color("ecoContainerHi", bundle: .designSystem)
    static let onEcoContainerLow = Color("onEcoContainerLow", bundle: .designSystem)
    static let onEcoContainerHi = Color("onEcoContainerHi", bundle: .designSystem)
    static let eco = Color("eco", bundle: .designSystem)
    static let onEco = Color("onEco", bundle: .designSystem)
    static let outlineEco = Color("outlineEco", bundle: .designSystem)

    // MARK: - Family

    static let familyContainerLow = Color("familyContainerLow", bundle: .designSystem)
    static let familyContainerHi = Color("familyContainerHi", bundle: .designSystem)
    static let onFamilyContainerLow = Color("onFamilyContainerLow", bundle: .designSystem)
    static let onFamilyContainerHi = Color("onFamilyContainerHi", bundle: .designSystem)
    static let family = Color("family", bundle: .designSystem)
    static let onFamily = Color("onFamily", bundle: .designSystem)
    static let outlineFamily = Color("outlineFamily", bundle: .designSystem)

    // MARK: - Gradient

    static let gradientPrimaryHiStart = Color("gradientPrimaryHiStart", bundle: .designSystem)
    static let gradientPrimaryHiEnd = Color("gradientPrimaryHiEnd", bundle: .designSystem)
    static let gradientPrimaryLowStart = Color("gradientPrimaryLowStart", bundle: .designSystem)
    static let gradientPrimaryLowEnd = Color("gradientPrimaryLowEnd", bundle: .designSystem)
    static let gradientSecondaryStart = Color("gradientSecondaryStart", bundle: .designSystem)
    static let gradientSecondaryEnd = Color("gradientSecondaryEnd", bundle: .designSystem)
    static let gradientWhiteMin = Color("gradientWhiteMin", bundle: .designSystem)
    static let gradientWhiteMax = Color("gradientWhiteMax", bundle: .designSystem)
    static let gradientBlackMin = Color("gradientBlackMin", bundle: .designSystem)
    static let gradientBlackMid = Color("gradientBlackMid", bundle: .designSystem)
    static let gradientBlackHi = Color("gradientBlackHi", bundle: .designSystem)
    static let gradientBlackMax = Color("gradientBlackMax", bundle: .designSystem)

    // MARK: - Link

    static let link = Color("link", bundle: .designSystem)

    // MARK: - Loyalty

    static let loyaltyContainerLow = Color("loyaltyContainerLow", bundle: .designSystem)
    static let loyaltyContainerHi = Color("loyaltyContainerHi", bundle: .designSystem)
    static let onLoyaltyContainerLow = Color("onLoyaltyContainerLow", bundle: .designSystem)
    static let onLoyaltyContainerHi = Color("onLoyaltyContainerHi", bundle: .designSystem)
    static let loyalty = Color("loyalty", bundle: .designSystem)
    static let onLoyalty = Color("onLoyalty", bundle: .designSystem)
    static let outlineLoyalty = Color("outlineLoyalty", bundle: .designSystem)

    // MARK: - Loyalty Status

    static let classic = Color("classic", bundle: .designSystem)
    static let silver = Color("silver", bundle: .designSystem)
    static let gold = Color("gold", bundle: .designSystem)
    static let platinum = Color("platinum", bundle: .designSystem)
    static let diamond = Color("diamond", bundle: .designSystem)
    static let limitless = Color("limitless", bundle: .designSystem)

    // MARK: - Offer

    static let offerContainerLow = Color("offerContainerLow", bundle: .designSystem)
    static let offerContainerHi = Color("offerContainerHi", bundle: .designSystem)
    static let onOfferContainerLow = Color("onOfferContainerLow", bundle: .designSystem)
    static let onOfferContainerHi = Color("onOfferContainerHi", bundle: .designSystem)
    static let offer = Color("offer", bundle: .designSystem)
    static let onOffer = Color("onOffer", bundle: .designSystem)
    static let outlineOffer = Color("outlineOffer", bundle: .designSystem)

    // MARK: - Outline

    static let outlinePrimaryHi = Color("outlinePrimaryHi", bundle: .designSystem)
    static let outlinePrimaryLow = Color("outlinePrimaryLow", bundle: .designSystem)
    static let outlineSecondary = Color("outlineSecondary", bundle: .designSystem)
    static let outlineLow = Color("outlineLow", bundle: .designSystem)
    static let outlineMid = Color("outlineMid", bundle: .designSystem)
    static let outlineHi = Color("outlineHi", bundle: .designSystem)

    // MARK: - Overlay

    static let overlayLow = Color("overlayLow", bundle: .designSystem)
    static let overlayMid = Color("overlayMid", bundle: .designSystem)
    static let overlayHi = Color("overlayHi", bundle: .designSystem)
    static let overlayMax = Color("overlayMax", bundle: .designSystem)

    // MARK: - Primary

    static let primaryContainerLow = Color("primaryContainerLow", bundle: .designSystem)
    static let primaryContainerHi = Color("primaryContainerHi", bundle: .designSystem)
    static let accorPrimary = Color("primary", bundle: .designSystem)
    static let onPrimary = Color("onPrimary", bundle: .designSystem)
    static let onPrimaryContainer = Color("onPrimaryContainer", bundle: .designSystem)

    // MARK: - Secondary

    static let secondaryContainer = Color("secondaryContainer", bundle: .designSystem)
    static let onSecondaryContainer = Color("onSecondaryContainer", bundle: .designSystem)

    // MARK: - Shadow

    static let shadowDefault = Color("shadowDefault", bundle: .designSystem)
    static let shadowStrong = Color("shadowStrong", bundle: .designSystem)

    // MARK: - State

    static let focus = Color("focus", bundle: .designSystem)

    // MARK: - Success

    static let successContainerLow = Color("successContainerLow", bundle: .designSystem)
    static let successContainerHi = Color("successContainerHi", bundle: .designSystem)
    static let onSuccessContainerLow = Color("onSuccessContainerLow", bundle: .designSystem)
    static let onSuccessContainerHi = Color("onSuccessContainerHi", bundle: .designSystem)
    static let success = Color("success", bundle: .designSystem)
    static let onSuccess = Color("onSuccess", bundle: .designSystem)
    static let outlineSuccess = Color("outlineSuccess", bundle: .designSystem)

    // MARK: - Surface

    static let surface = Color("surface", bundle: .designSystem)
    static let surfaceContainerLow = Color("surfaceContainerLow", bundle: .designSystem)
    static let surfaceContainerMid = Color("surfaceContainerMid", bundle: .designSystem)
    static let surfaceContainerHi = Color("surfaceContainerHi", bundle: .designSystem)
    static let onSurfaceLow = Color("onSurfaceLow", bundle: .designSystem)
    static let onSurfaceMid = Color("onSurfaceMid", bundle: .designSystem)
    static let onSurfaceHi = Color("onSurfaceHi", bundle: .designSystem)

    // MARK: - Warning

    static let warningContainerLow = Color("warningContainerLow", bundle: .designSystem)
    static let warningContainerHi = Color("warningContainerHi", bundle: .designSystem)
    static let onWarningContainer = Color("onWarningContainer", bundle: .designSystem)
    static let warning = Color("warning", bundle: .designSystem)
    static let onWarning = Color("onWarning", bundle: .designSystem)
    static let outlineWarning = Color("outlineWarning", bundle: .designSystem)

    // MARK: - Other

    static let brandLogos = Color("brandLogos", bundle: .designSystem)
    static let neutralContainer = Color("neutralContainer", bundle: .designSystem)
    static let onNeutralContainer = Color("onNeutralContainer", bundle: .designSystem)
    static let watermark = Color("watermark", bundle: .designSystem)
}

public extension Color {
    static func dynamic(light: UIColor, dark: UIColor) -> Color {
        Color(UIColor { trait in
            trait.userInterfaceStyle == .dark ? dark : light
        })
    }
}
